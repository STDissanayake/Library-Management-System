# = Server Configuration
server.port=8080

# = Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/databasetest1?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=It24101867!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# = JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# = Logging
logging.level.org.springframework=INFO
logging.level.lk.library.library_management_system=DEBUG

# = Custom App Settings
app.name=Library Management System
app.version=1.0
